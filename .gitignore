# dependencies (both packages)
node_modules/

# secrets — every developer brings their own .env (template: the-pack-main/guide/thepack.env)
.env
.env.*

# build output & caches
.next/
*.tsbuildinfo

# logs & OS junk
*.log
.DS_Store
Thumbs.db
desktop.ini

# local tooling state
.claude/
