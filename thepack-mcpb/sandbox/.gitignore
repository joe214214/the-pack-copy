# holds your API keys — never commit
.env

# a COPY of your Claude login token + skills, seeded by start.sh/.bat — never commit
claude-home/

# Copy of the owner's Hermes login (auth.json / nous_auth.json) — never commit.
hermes-home/

# Copy of the owner's Codex login (auth.json) — never commit.
codex-home/
